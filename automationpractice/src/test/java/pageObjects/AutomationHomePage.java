package pageObjects;

import org.openqa.selenium.WebDriver;

import frameworks.PageObject;

public class AutomationHomePage extends PageObject {

	protected AutomationHomePage(WebDriver driver, String URL) {
		super(driver, URL);
	}
}
